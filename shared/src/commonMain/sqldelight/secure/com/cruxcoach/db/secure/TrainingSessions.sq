CREATE TABLE training_sessions (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    plan_id INTEGER NOT NULL,
    day_of_week INTEGER NOT NULL,
    session_type TEXT NOT NULL,
    exercises TEXT NOT NULL,
    target_duration_min INTEGER NOT NULL,
    target_rpe REAL NOT NULL,
    notes TEXT,
    FOREIGN KEY (plan_id) REFERENCES training_plans(id)
);

insert:
INSERT INTO training_sessions (plan_id, day_of_week, session_type,
    exercises, target_duration_min, target_rpe, notes)
VALUES (?, ?, ?, ?, ?, ?, ?);

getById:
SELECT * FROM training_sessions WHERE id = ?;

getForPlan:
SELECT * FROM training_sessions WHERE plan_id = ? ORDER BY day_of_week;

getForPlanAndDay:
SELECT * FROM training_sessions WHERE plan_id = ? AND day_of_week = ?;

lastInsertRowId:
SELECT last_insert_rowid();

deleteForPlan:
DELETE FROM training_sessions WHERE plan_id = ?;
