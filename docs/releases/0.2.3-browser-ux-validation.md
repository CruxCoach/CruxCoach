# 0.2.3 browser, filters and guided entry

Work starts from release `35920004e` on `feat/0.2.3-browser-ux`.

## Implementation

- One browser header row: app logo, board, angle, Bluetooth and filter.
  All remain visible; model/size details may truncate while the board family
  remains readable. Logbook, lists, settings and replay live in the logo menu. The angle
  sheet uses supported board angles; resetting filters preserves the angle.
- Filters prioritise grade, sent exclusion/status and sorting. Further restrictions
  are expandable; choice lists are vertical. Large-text layouts retain access to
  reset, angle controls and results.
- Setup offers optional BLE connection before board picking. Discovery suggests a
  known board family without hiding alternatives or inferring layout/size/angle.
  Catalogue choices stay expanded. Account restoration lives in the subsequent
  CruxCoach import section, alongside the existing Kilter and MoonBoard paths.
- Setup skip suppresses the tour and does not silently select all catalogues.
- The tour dims the screen around the measured bounds of the real control. Short
  adjacent hints and a pointer show where to tap. No duplicate connect/next/done
  actions, tutorial cards, synthetic taps or automatic log entries are used.
- Actual Bluetooth/angle/filter actions advance the tour. Closing the connection
  sheet allows progress without hardware. Primary header controls remain direct
  even on narrow screens; the logo menu contains the secondary destinations.
- The detail step uses the actual connect/light/queue control, then guides to the
  actual Quicklog attempt and Top buttons simultaneously. The hint explicitly
  states that tapping saves a real entry. Only successful persistence advances
  the tour, which remembers the entry UUID and follows the normal back/logo/menu
  path to its edit action in the logbook. Editing is optional, and deletion is
  available in the edit dialog with a separate confirmation. Skipping never logs
  an entry. The space between the Quicklog buttons remains blocked.
- Tour dismissal persists. Existing installations do not automatically start it;
  replay is available from the app menu behind the logo.

## Evidence before the spotlight revision

On API 35 / German / 1080×1920 / 420 dpi, candidate `1f377ca96` (feature build
1000013) was installed after validator receipt, APK SHA-256, package/version and
Android certificate checks. ADB verified setup skip without downloads, tour replay,
angle 40° → 35°, filter reset preserving angle, vertical sort/source choices,
status multiselection, active-filter count, zero-result recovery, real catalogue
browsing and detail navigation. The tour could finish without logging an ascent
and remained dismissed after restarting the app.

A fresh start of the isolated test package exercised nearby-device permission,
Bluetooth enablement and discovery. No physical board was available. Original
Bluetooth/display settings were restored. Other installed apps were not cleared.

Large-text checks found clipped tour and angle controls and a wrapping filter
header; those findings led to the accessibility fixes. The later spotlight
revision replaces the tour cards entirely and needs its own device verification.

Candidate `f29b780297885a63f974bd589ff250a2317da041`, feature build 1000015,
passed full source CI (`35321037291`) and the trusted publisher (`35321538115`,
published/receipt-delivered gate). Its validator-signed receipt and APK were
verified before installation. APK SHA-256:
`4474e500b9dc8954eb1702b5238012bdecd3f5d511134aa0d40a0128dbef0980`.
This candidate contains the expanded catalogue and accessibility refinements;
it predates the owner's requested spotlight tour.

## Spotlight revision — validation pending

Eleven focused tests passed (`/tmp/browser-ux-spotlight-tests3.log`). They exercise real touch delivery through the highlight to the original
button, skipping at large font size, narrow-header overflow routing, tour state
persistence and onboarding download consent. Record results and final signed
candidate/device screenshots here before advancing the release branch.

No stable publication or main merge is performed. Physical BLE lighting and a
production-signed 0.2.2 → 0.2.3 upgrade are outside the evidence above.

## Follow-up: focused input and explicit board configuration

The spotlight now blocks touches and drags outside its measured target. Popup
menus disable unrelated actions and retain a skip action. The actual highlighted
control still handles the input. The one-row browser toolbar also displays the
physical size/configuration below the board name; the picker echoes the complete
selection alongside a smaller preview, with wrapping rather than truncation.

Focused `BrowserUxTest`, `BoardBrowserHeaderTest`, and `BoardSelectionLabelTest`
passed (`/tmp/browser-ux-input-shield-tests.log`). Coverage includes background
touch/drag blocking, target/skip input at 200% font size, and disabled unrelated
menu actions with narrow-header overflow routing.

The preceding spotlight candidate `e2fbaf61565798fb2fbc3065a52e98cad069c9b3`
(feature build 1000016) passed source CI 35323439052 and trusted publication
35323965266. Its validator receipt, APK hash, package/version and certificate
were verified before ADB installation. On-device replay opened the real Android
Bluetooth prompt; declining it advanced the tour to the angle control. The new
input blocking and board summary require the next published candidate for ADB
validation; this record does not claim they are already installed.

## Quicklog / permanent header revision

- The family label is measured against available width; only secondary board
  detail is ellipsized. Logo, angle, Bluetooth and filters keep their touch areas.
- Tour input blocking supports two separate targets without opening the space
  between them. Quicklog advances only on a successful save and persists the
  saved UUID across process recreation; replay clears that UUID.
- The logbook highlights the matching entry rather than an arbitrary first row.
  It scrolls to that entry and permits opening the regular edit dialog. If the
  saved entry no longer exists, the tour ends without pointing at another record.
- Attempt editing now calls updateBid and uses the attempt form; send editing
  continues to use updateAscent. The regular edit form also offers single-entry
  deletion behind a confirmation, using the correct attempt/send repository path.
- Device verification completed on the published candidate below.

Focused validation passed: 26 tests across `BrowserUxTest`,
`BoardBrowserHeaderTest`, `BrowserTourTest`, `BoardLogbookOwnPublishTest` and
`AscentLoggerQuickLogTest` (`/tmp/browser-quicklog-tests5.log`). UI checks use
native graphics, inspect the painted family-label bounds at large font size,
and wait for asynchronous drawer clicks. Both Quicklog targets, blocked middle
and background controls, real menu navigation, UUID persistence, attempt/send
editing and existing Quicklog/undo behavior are covered. No device logs were
created during these JVM checks.


## Published candidate — device validation, 2026-09-18

Installed feature build **1000019**, version 0.2.3, source
`6e97f0de02434332c047114d33ad2e8fd03e93e5`, package
`com.cruxcoach.android.dev.f_0bee1614a15c`.
[Source CI](https://github.com/CruxCoach/CruxCoach/actions/runs/35362566649)
and [trusted publisher](https://github.com/CruxCoach/CruxCoach/actions/runs/35363332601)
succeeded. The publisher enforces published/receipt-delivered success. The
validator receipt signature, source identity, APK hash, Android certificate,
package and version were independently checked before installation.
APK SHA-256: `8e5b23f0ef799ab083e450c297b405a59f63022dcbd3402f4386b75ae9e08f0b`.

ADB checks on API 35 / German / 1080×1920:

- One-row toolbar keeps logo, board selection, angle, BLE and filters visible.
  Picker shows the complete Kilter Original / 12x12 with Kickboard configuration.
  Introduction replay uses the circled info icon in the logo menu.
- Replay follows actual BLE, angle, filter, climb and connect controls. Declining
  Bluetooth and closing discovery permits continuing without physical hardware.
  Background climb taps and drags are blocked during the spotlight.
- Both real Quicklog controls are highlighted; the intervening connect control
  remains blocked. One explicitly created test attempt advances to Back, logo,
  Logbook and the exact saved entry's edit pencil. Unrelated drawer actions stay
  blocked. No successful send was created during this device test.
- Editing the attempt count from one to two retains the attempt outcome and zero
  sends. Cancelling deletion preserves it; confirming deletion removes it.
  The initially empty test logbook is empty again after cleanup.
- At 320dp width and 200% font scale, all primary toolbar controls remain visible,
  the family name stays readable, and filters, replay and skip remain usable.
  Display density was reset to its original 420dpi and font scale to 1.0.
  Bluetooth remains off and the selected angle remains 30 degrees.
- Force-stop/relaunch returns to the browser without replaying the dismissed
  tour, confirmed by screenshot. UIAutomator initially returned a null root
  during cold start; no stale hierarchy was used.

Screenshots retained on the development host under
`/tmp/cruxcoach-browser-ux/`: `quicklog-header.png`,
`quicklog-board-picker.png`, `quicklog-info-menu.png`,
`quicklog-filter-hint.png`, `quicklog-choice.png`,
`quicklog-logbook-entry.png`, `quicklog-header-font200-320dp.png`,
`quicklog-filter-font200-320dp.png`, `quicklog-menu-font200-320dp.png`,
`quicklog-tour-font200-320dp.png` and `final-restart.png`.

Device review found plural batch-delete wording in the single-entry confirmation.
The follow-up changes only that confirmation to dedicated singular EN/DE strings;
XML parsing and unique resource-key checks passed. This wording correction is
newer than installed build 1000019 and still needs the next CI-built APK.
Physical BLE lighting and a production-signed 0.2.2 to 0.2.3 upgrade remain
untested; this record does not certify the stable release.


## Follow-up: compact picker and responsive overflow

Owner review of build 1000019 requested that secondary toolbar actions use a
right-hand overflow rather than the logo drawer. A fresh ADB screenshot
(`/tmp/cruxcoach-browser-ux/topbar-before-overflow.png`) confirmed the board
picker expanded across unused space.

The picker is now capped at 132dp. Logo, board family, angle, Bluetooth and
filters stay directly accessible; remaining width admits Logbook, Lists and
Settings in that order, with hidden actions in the right-hand “…” menu.
The angle retains a 48dp touch target. At the test phone's approximately 411dp
width, Logbook fits directly and Lists/Settings use overflow. On narrower or
large-font layouts, Logbook can use overflow too. The logo drawer no longer
contains these secondary toolbar actions; it retains board navigation, map and
introduction replay. The logbook tour targets the actual direct/overflow action;
unrelated overflow entries are disabled during that step and Skip remains usable.

The preceding installed-candidate evidence applies to build 1000019. The new
layout requires a newly published APK for a matching device screenshot.

Focused checks passed: 12 tests in `BrowserUxTest` and `BoardBrowserHeaderTest`
(`/tmp/browser-overflow-tests4.log`). Coverage includes direct primary controls
at 320dp with large fonts, a real 411dp test viewport for compact picker/direct
Logbook, hidden-action routing and tour restrictions in overflow. Both translated
resource XML files parse successfully.


### Published overflow candidate and vertical alignment follow-up

Feature build **1000021**, source `d2dd64cf539f4f9262ae90fd2dab53858976a610`,
passed [source CI](https://github.com/CruxCoach/CruxCoach/actions/runs/35372116515)
and [trusted publication](https://github.com/CruxCoach/CruxCoach/actions/runs/35372753947).
The signed validator receipt, APK SHA-256, package, version and certificate were
checked before successful ADB installation. APK SHA-256:
`2fa56f5d56775b7eac33912a34d2ee5327a38258e1aaafb33293a025eecd991f`.

At the original 420dpi / font scale 1.0, the real device shows the compact picker,
all primary controls and a direct Logbook icon. The right-hand overflow contains
Lists and Settings. Opening Lists through overflow and the empty Logbook through
its direct icon both succeeded. Screenshot: `/tmp/cruxcoach-browser-ux/compact-overflow.png`.
No log entries were created. A subsequent screenshot timed out and the ADB bridge
returned protocol faults before the settings, logo drawer and large-font device
checks could run. Display settings had not yet been changed.

Owner review identified the board label's top alignment inside its 48dp touch
area. The follow-up centers the entire text-and-chevron row vertically within
that area, aligning it with the other toolbar controls without changing widths
or navigation. This correction is newer than installed build 1000021 and needs
the next published APK for device confirmation.

After the centering correction, all 12 focused header/browser UI tests passed
again (`/tmp/browser-picker-centering-tests.log`).


### Centered picker verified on build 1000022

Source `8d488b53e10450a83875485bec15cfa70a89586c` passed source CI
35379305091 and trusted publisher 35379944364. Its validator receipt, APK hash,
package/version and certificate were verified before ADB installation.
APK SHA-256: `1e05977e8eb2f6e99fbb7b36cf4c4bc8d07f701a153e6f8f878a2c2046162595`.

Screenshot and hierarchy confirm the picker text block is centered at the same
vertical coordinate as the toolbar icons. Settings opens through overflow;
the logo drawer contains no duplicate Logbook/Lists/Settings actions. At 320dp
and 200% font, primary controls and family remain visible, and Logbook opens
through overflow. After the display change initially exposed the launcher, the
check was repeated with an explicit app launch and passed. Original 420dpi and
font scale 1.0 were restored; the user's current 40-degree selection was retained.
No log entries were created or deleted.

Screenshots under `/tmp/cruxcoach-browser-ux/`: `centered-topbar.png`,
`centered-overflow.png`, `centered-topbar-font200-320dp.png`,
`centered-overflow-font200-320dp.png`. The subsequent owner-requested change from
horizontal to vertical overflow dots is icon-only and newer than this APK.

### MoonBoard hold sets in the board picker

The shared picker now offers “Missing some holds?” for MoonBoard variants with
multiple hold sets. All sets remain the default; existing per-variant subsets
are loaded and open the extended selection. The dialog keeps edits in local
per-variant drafts. Confirmation writes the selected variant and hold sets in
one preferences transaction, after any required download consent. Cancel does
not persist the draft. At least one valid set must remain selected. Missing
catalogue hold-set metadata is explained in the expanded controls.

The old hold-set section and standalone connection-information heading are no
longer shown in MoonBoard settings. Existing LED-position controls remain.
This MoonBoard change and the vertical overflow icon require the next published
APK for ADB validation; build 1000022 verifies the preceding centering change.

All 33 focused tests passed (`/tmp/moon-picker-tests3.log`): picker drafts,
confirmation/cancellation, minimum selection, per-layout preferences, atomic
board/hold-set writes, and existing download-consent behavior. EN/DE resource
XML and diff whitespace checks passed.


## 2026-09-18 — Account recovery and data backup

The account/key export and import flows now explain that saving an nsec preserves
account access and does not create a data backup. They link directly to backup
settings, where activation, the first upload and automatic cadence remain explicit.
Importing a different identity resets backup consent and cadence to disabled/manual.
Restore remains available without enabling uploads. Amber-only accounts can access
backup controls. English and German copy describe public-server metadata and the
limits of remote deletion, including the completion heading.

Backup key-publication failures now propagate before a new pointer is advertised;
HTTP upload failures no longer read or log untrusted response bodies. This is bounded
hardening, not a comprehensive security clearance. The owner received a separate
local review with outstanding lifecycle/multi-device recovery risks; those need
resolution before claiming reliable recovery across every supported scenario.

Validation: 68 focused JVM/Robolectric tests passed (crypto, compression, pointer and
event validation, HKDF, publication failure propagation, response privacy, identity
consent reset, account UI and backup dialogs). Device validation of these new changes
is pending a published feature APK. No real nsec was inspected and no real backup was
uploaded or removed during this review.

Additional validation: 37 focused tests passed for direct backup navigation and
back/recreation behavior, backup dialogs, secure data and own-climb round trips.


## 2026-09-18 — Installed backup review and unified key storage flow

Installed feature build **1000024**, source `fbd5a713bab3e096fc3d170831b7300371fca14b`,
on `PL2GAR9841808297` over ADB without clearing application data. Source CI
`35384496168` and trusted publisher `35385104954` succeeded. The retrieved validator
receipt signature, APK SHA-256 (`42e75b09ef3610bf98a64c84a4fd3351b559e7d87131a07b6086f64e5df002fc`),
package, development signing certificate and versionCode were verified. This does
not claim independent verification of the referenced Root policy event.

Device observations: account-to-backup button opens the correct page; Back restores
the account page and scroll. The pre-copy warning clearly distinguishes key and data
backup. The remote-delete explanation is legible and cancellation leaves it untouched.
No private key was revealed/copied, no identity was imported, and no backup was
activated, uploaded, restored or deleted. Screenshots of the non-secret backup page
and deletion warning are under `/tmp/cruxcoach-browser-ux/backup-*-build1000024.png`.
The cold/warm `backup_settings` intent incorrectly returned to the browser because
NavGraph lacked the route in its dispatch allowlist; this follow-up fixes that path.
The disabled/no-history text also no longer claims there is no remote data.

The following source changes supersede the two-dialog flow in that installed build:
a single dialog keeps the pending backup choice while the user authenticates, copies
the key and stores it externally. The explicit storage confirmation is the first
point that can upload. Successful first upload enables daily scheduling for a new
opt-in; existing settings are preserved. Failures and retries stay inside the dialog.
Already-stored keys enter the same confirmation flow, without falsely saying they
were just copied. Cancelling authentication allows retry; copying again after the
clipboard timeout is available. No nsec is stored in the new ViewModel/state.

The backup page now keeps a short privacy explanation visible and places full
metadata/deletion details in its existing info disclosure. The new flow and the
route correction require a subsequent published APK for device verification;
1000024 is not evidence for them. See `design.md` for the design principles.

Final focused validation: **27 tests passed**, including eight state-machine tests
for consent, cancellation, feature availability, first-upload scheduling order, retry
and existing settings; two dialog tests include 320 dp / 200% font and inline
progress/error actions. Account UI, backup dialogs and settings navigation also pass.
Log: `/tmp/account-backup-final-tests.log`. Tests use synthetic state and mocked uploads.

## 2026-09-18 — Unified backup flow on device (build 1000025)

Installed **1000025**, source `3ae5ac151321eecaef6563bc0ce3d5447cb05ee0`, on the
existing test package without clearing data. Source run `35395743705` and publisher
`35396318395` succeeded. Validator receipt signature, version/package, development
certificate and APK hash `327d8b12fc5e4ff792719c4353529e91fe4e036b78a648689c33fc5842bc7b6b`
were verified. The referenced Root policy was not independently retrieved.

Observed with German UI: cold direct navigation to `backup_settings` now works.
Backup was already enabled with MANUAL cadence before testing; both were preserved.
The new key-copy entry opens one combined dialog with the existing backup setting
shown enabled/read-only. Home and return preserve the open dialog. Cancellation
returns without marking the key saved. The already-stored-key entry uses the same
dialog with the explicit final action, and correctly does not claim the key was
just copied. Both flows were cancelled before authentication or final confirmation.
No nsec was copied/read, no identity changed, and no backup was uploaded or deleted.

At 320 dp / 200% font, device UI bounds exposed a real action overlap in the
Material AlertDialog action layout: confirmation y=1024..1456, cancel y=1245..1425.
Merely finding both actions in the accessibility tree did not establish usability.
The follow-up places both actions in a single explicit vertical Column and strengthens
the regression test to require non-overlapping bounds for COPY and STORE states.
That layout correction is newer than the installed 1000025 and needs its own device
recheck after publication. Font scale 1.0 and physical density 420 were restored and
verified; backup cadence remained MANUAL. Non-secret backup-page screenshot:
`/tmp/cruxcoach-browser-ux/backup-page-1000025.png`. The key screen's FLAG_SECURE was
not bypassed to obtain images.

Focused verification of the correction: both `AccountBackupDialogTest` cases passed,
including the new non-overlap assertions at 320 dp / 200% font. Log:
`/tmp/account-backup-layout-tests.log`.

## Account and backup: progressive disclosure follow-up

The account screen now separates account access, data backup and restoring an existing
account. Local-key users see Amber as a collapsed optional section; active Amber
accounts keep their controls visible. Visible copy uses plain “key” terminology;
“nsec”, safe storage, clipboard expiry and public-server metadata are explained in
contextual info cards. The combined backup dialog retains the visible opt-in,
explicit storage confirmation, upload timing and daily-backup consequence.

The former large red key warning is a compact neutral reminder with a direct action
and optional explanation. Essential key-loss consequences stay visible. Backup status
and descriptions use normal body text rather than small print. Import instructions
and account overview help are also shorter. English and German resources agree.
No key handling, upload consent, scheduling or restore implementation was changed.

Focused account UI, backup flow, dialog and remote-delete tests passed in
`/tmp/account-compact-tests.log`; the final copy and read-only help interactions passed
in `/tmp/account-compact-final-tests.log`. The new help test checks that reading details
does not copy a key, navigate, acknowledge storage or upload, and preserves a pending
backup choice. Rendered account fixtures (including larger fonts) were visually
reviewed under `/tmp/cruxcoach-account-compact/`. These are synthetic Robolectric
renders, not screenshots of a newly published APK. Device verification awaits the
next feature publication; the installed 1000025 is an older UI.

The complete backup-page fixture was also visually reviewed (`backup-compact.png`);
its key-info interaction test passed in `/tmp/backup-compact-preview-test.log`.

## First onboarding screen: board selection before connection assistance

The first screen now leads with the selected board/model/size as one tappable
picker. Bluetooth is a subordinate optional action below it; it still only
suggests a family and does not infer physical size. Download families remain
expanded, using full-width multi-select rows with selection icons and accessible
checkbox semantics. The select-all row and repeated section copy are removed.
The persistent action says Download and continue for a nonempty selection and
Continue for an empty selection. Detail explanations use the existing info cards.
The step indicator and setup skip share one header row.

Download confirmation still happens only through Continue. No automatic download
is added on selection or Bluetooth connection. The separate defer-Bluetooth button
is removed; users can proceed without a connection and the later browser tour
retains its regular optional connection step.

Validation: 12 focused cases passed (`OnboardingDownloadSelectionTest` and
`BrowserUxTest`), including edited catalogue selection across UI recreation,
confirmation only after Continue, pending confirmation disabling Continue, and
skip without download at 320 dp / 200% font. English/German resource XML parses
without duplicate names; `git diff --check` passes. A Robolectric render is used
for visual review; it is not a device screenshot. ADB became reachable again,
but the new feature APK must publish before this UI can be verified on hardware.

## Inline board images and minimal setup (2026-09-19)

The detached picker preview is removed. Model/size options now pair their own
104 × 144 dp image with a wrapping name and radio selection. The image is an
independent, labelled zoom action with a visible expand icon; opening it never
selects or persists a board. Missing assets have an explicit placeholder.
Picker thumbnails decode at a bounded resolution, while fullscreen inspection
retains the original resolution and pinch/pan controls.

Onboarding download choices now have one Select all / Deselect all action and a
selection count. Setup skip is removed at the owner's request; Bluetooth and
import remain optional. Empty catalogue selection still continues without
starting a download. The earlier skip-button validation above is historical.

Validation: eight focused cases passed for picker cancel/draft/confirmation and
onboarding bulk selection/continuation; two additional native-render tests passed
for image zoom versus radio selection and preserving the confirmed variant while
inspecting another image. The updated onboarding test includes 320 dp / 200%
font. Rendered MoonBoard picker and first onboarding screen were visually
inspected. XML/resource uniqueness and whitespace checks pass. These are local
Robolectric checks; installation/physical-device inspection of the new APK is
pending publication (ADB intermittently reports a protocol fault).

## Second onboarding screen: public catalogues and private logbooks

The second step has two explicit sections: Board databases and Private climbing
data. The compact download card reports the actual state, selected-catalogue
readiness and relevant recovery action. Technical diagnostics and per-board
progress are available on demand; this detail list excludes unselected families.
No-network status no longer promises an automatically queued Wi-Fi download.
An active import cannot display the overall Ready state merely because old
catalogue rows exist. Selection editing uses a compact action beside Details.

Private imports are optional accordions: each source opens its controls directly
below its own row. Opening/closing a source does not start a login or import.
The app-start action remains available during background catalogue downloads.
Info cards explain public versus private data, offline readiness and later import.

Validation: 12 distinct focused cases passed across `BoardCatalogueStatusTest`,
`OnboardingDataStepTest` and `OnboardingDownloadSelectionTest`. Covers offline
versus scheduled download wording, selected-only detail rows, stale counts during
sync, visible per-board errors despite old data, source accordion behavior without
side effects, and continuation during downloads. Rendered German views at normal
size and 320 dp / 200% font were inspected. The long heading and narrow footer
buttons exposed by the first render were corrected: shorter Board catalogues
heading and full-width stacked actions at large font sizes. App start uses a short
label. XML/duplicate resource checks and `git diff --check` pass. Device testing
of this revision remains pending its published APK.

## Beta thumbnails retain their decoded cache across scrolling

Lazy rows previously reset their verified-file state to null on recreation and
ran disk/hash verification before Coil could see the image. The gallery now hands
Coil a stable typed request keyed by verified content hash. A matching memory
entry is also the immediate placeholder, with crossfade disabled. Verification
and file/network work run only inside the custom fetcher after a memory miss.
There is no unrestricted URL-fetcher fallback; allowed-host/hash/known-logo checks
still happen before requesting even a cached image. The existing bounded image
memory cache and verified disk cache remain in use.

Validation: 11 focused cases passed across BetaThumbnailUrlsTest,
BetaVideoLinkIntentTest and the new BetaThumbnailCacheTest. The two new native
image-loader tests verify fresh-request/mirror memory hits without re-entering
verification (even after disk deletion), disk fallback after memory eviction,
and failure without bypassing verification. `git diff --check` passes. Actual
scrolling on the phone with this revision remains pending the published APK.

## Tour eligibility and visible exit

Existing startup already defaulted missing tour preferences to INACTIVE and only
started a tour at setup completion or explicit replay. Updates therefore did not
start a tour for everyone. The setup handoff now additionally checks the existing
identity-scoped completion flag and installation-level profile cache. Re-entering
setup cannot start a tour for an established installation; non-replay starts also
preserve a running or completed/dismissed tour. Explicit replay remains available.

The spotlight exit is now a white filled button with black text and a close icon,
centered on the edge opposite the highlighted controls. Actual button height is
reserved in hint placement for both upper and lower targets. Tour popup menus
also expose a separated, filled exit action. No extra confirmation is introduced.

Validation: 18 distinct focused cases passed (`BrowserTourTest`, `BrowserUxTest`,
`TourSkipLayoutTest`). New cases cover fresh versus existing setup, installation
history after identity-scoped setup reset, preserving running/dismissed progress,
explicit replay and nonoverlapping touchable exits for top/bottom targets at
320 dp / 200% font. Both rendered spotlight positions were visually inspected.
Whitespace checks pass. Device inspection of this revision awaits publication.

## Onboarding selection cleanup

Removed the visible download description and selection-count row. Select all /
Deselect all now shares the catalogue heading row. Bluetooth discovery moved
from the first page into its board picker, next to the category controls. Opening
discovery closes the picker; dismissing discovery returns to the picker seeded
with the recognized family suggestion. Other families remain selectable and this
handoff does not confirm the board or start catalogue downloads.

Picker choice cards use a neutral background, retaining the orange selection
border and radio indicator. Image expand badges are removed; tapping the image
still opens fullscreen zoom and its accessible action label remains. Onboarding
catalogue selection uses a subtle orange tint instead of the teal fill.

Validation: seven focused cases passed (OnboardingDownloadSelectionTest,
BoardImageChoiceTest, MoonBoardPickerDraftTest), covering large-text catalogue
selection, the in-picker Bluetooth action without confirmation, image zoom,
independent board selection and hold-set draft semantics. Normal onboarding and
picker renders were inspected: the catalogue title and bulk action share one row,
and selected picker cards retain a neutral fill without expand badges. Whitespace
checks pass. Physical Bluetooth discovery and device inspection await publication.

## Editable catalogue queue and shared selection (2026-09-19)

The catalogue selector stays enabled during downloads on setup step 2 and in
settings. Its rows share the first setup screen's implementation. Taps edit a
draft; cancel discards it, confirmation persists it before scheduling additions.
Confirmed additions share a single follow-up after the active sync, using the
latest saved selection. Unchanged or reduced selections do not enqueue more work;
clearing the choice before the follow-up starts suppresses that follow-up.
Deselecting does not interrupt the active import or delete local data.

The dialog has a bulk action, optional info card, one short timing hint and fixed
confirmation/cancel actions outside the scroll area. Destructive data-deletion
selectors retain their separate confirmation behavior. Settings places catalogues
next to board/Bluetooth; selection and status precede the update interval.

Focused validation: BoardDownloadOnboardingTest (5), OnboardingDownloadSelectionTest
(2), OnboardingDataStepTest (2), SettingsNavigationTest (5), CatalogueSelectionFlowTest
(2), BoardSyncSelectionQueueTest (2). Tests exercise draft cancellation, confirm-only
saving, live-sync editing, coalesced follow-ups, opt-out and existing navigation.
Robolectric renders are development UI evidence, not device or published-APK
validation. `dadb devices` returned an empty list during this change; a device
check of the published feature APK remains pending.

## Board-specific settings cleanup (2026-09-19)

Only Kilter (LED colors) and MoonBoard (LED position) appear in the board-specific
settings selector. Other active families fall back to displaying Kilter settings
without changing the active board. Removed the inactive-board configure/activate
block and the standalone connection-lifecycle heading. Shared connection controls
remain in the all-boards section. English/German help now describes the actual
available options. Focused checks: SettingsBoardGroupingTest and BoardHubComposeTest.

## Import copy and picker spacing (2026-09-19)

Catalogue bulk selection sits immediately before the info affordance. The board
picker embeds its category label in the dropdown and reduces header spacing.
CruxCoach restore details move into the existing info card; the backup-enablement
consequence stays visible in one sentence. Kilter authentication/privacy and
one-time versus ongoing import explanations share the info dialog in onboarding
and account settings. Import actions remain distinct, with readable status and
errors. Onboarding's two Kilter import actions stack vertically.

The key-import page, CruxCoach file preview, MoonBoard device/CSV import and Aurora
migration use optional help for background explanations. Account changes, local-key
replacement, import failures and the Moon device-use instruction remain visible.
No import, permission, credential storage or backup activation logic changed.

Validation: nine focused UI tests passed (OnboardingDataStepTest,
OnboardingDownloadSelectionTest, ImportSourceCardTest, CatalogueSelectionFlowTest,
BoardImageChoiceTest). The restore-info check verifies access to the full
consequence text without starting key import. Reviewed rendered setup, catalogue
dialog, picker and expanded CruxCoach import views. ADB returned no devices;
published-APK/device verification remains pending.
