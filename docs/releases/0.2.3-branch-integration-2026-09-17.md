# 0.2.3 branch integration audit — 2026-09-17

Baseline: the shipped `v0.2.2` tag is `18fb0b4f5` (2026-08-29).
The owner requested consolidation of genuinely new post-release work into
`feat/0.2.3-release`, preserving older worktrees and excluding older-origin work.
Branch names and recent merge dates alone do not establish eligibility.

## Integrated in this pass

- Fast-forwarded the local release from `2b4607898` to its existing remote
  `6fff3dc27`, retaining the MoonBoard coroutine-test correction and key-backup UX.
- Board download selection: source `50da549cdbe7de1ed782fe2e1d2bc0105f26f25d`,
  squashed as `92ea49c81`. Includes selection, onboarding/picker consent and actual
  catalogue availability/import status.
- Kilter upload: source `2cc45231de3b964a0263038445d743122ca531c8`,
  squashed as `c5133eabf`.
  Includes correct legacy climb IDs, retry reconciliation, compact status and
  opt-in diagnostics; also update reminder cadence, import rollback and send
  timeline correction. The `fix/0.2.3-kilter-upload` branch is a subset.

Integration uses squash commits to preserve the release line's existing linear
history. Both sides of the English/German resource additions are retained.
The active-board completeness guard still respects the download selection.

## Already included; no duplicate merge

| Source | Existing integration |
| --- | --- |
| Log-screen design `6d4f4748f` | `9a26a79ad`, whose body records source revisions and navigation decisions |
| Navigation B, MoonBoard beta videos, duplicate-key correction | Recorded as sources in `9a26a79ad`; the selected compact navigation is retained |
| Settings UX `6656e9e61` | `4e7f90233`, exact source recorded in its body |
| Repository guide `588434f8a` | `86b670c28`, reapplied documentation with release-checklist link |
| Owned Blossom thumbnails `54602e17a` | Direct ancestor of release |

`git cherry` alone cannot detect these previous squash integrations.

## Excluded by origin

`feat/marmot-permissions-v023` was created from the preserved 0.2.2 permissions
line (`928c5bd30`, August 17), then rebased/squashed onto newer release work.
Its September name/history does not make the underlying feature post-0.2.2 work.
Competitions, key custody, personal sharing and BoardCell/FIPS work likewise
predate the shipped baseline. Their branches and worktrees remain untouched.

## Explicitly excluded by the owner

Two branches began after the shipped baseline but are not explicitly 0.2.3
integration branches: `feat/cross-platform-refactor` (`44b0beaca`, 87 commits
outside release ancestry) and `feat/ai-board-image-prototype` (`5e3f93ad7`,
design assets/documentation). The owner explicitly chose to exclude both and include only the clear
0.2.3 changes. Their worktrees remain untouched.

## Validation boundary

Previous Kilter branch results are documented in
[the follow-up validation](0.2.3-validation-2026-09-17.md). They do not certify
this combined revision. Full CI/publication and a device run must use the final
combined release candidate. No main merge or stable publication is performed.

Combined-source validation: **116 focused tests passed**, zero failures/errors/
skips. Selection covers board download choice/consent/onboarding/status, importer
rollback, Kilter upload HTTP/status/diagnostics and update reminder throttling.
The first run exposed asynchronous ViewModel cleanup escaping the new download
tests into a subsequent updater test. Both new ViewModel test fixtures now await
`cancelAndJoin()` before resetting Main; the entire same selection passed on
rerun. English/German XML parses without duplicate resource names, and
`git diff --check` passes. These are local integration checks, not full CI or
an Android device/production upgrade certification.
